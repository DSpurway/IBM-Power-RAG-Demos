apiVersion: v1
kind: Service
metadata:
  name: vllm-service
  labels:
    app: vllm-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: vllm-service

# Made with Bob
