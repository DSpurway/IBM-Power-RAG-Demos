apiVersion: v1
kind: Service
metadata:
  name: llama-service
  labels:
    app: llama-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: llama-service

# Made with Bob