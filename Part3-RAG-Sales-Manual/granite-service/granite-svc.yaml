apiVersion: v1
kind: Service
metadata:
  name: granite-service
  labels:
    app: granite-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: granite-service

# Made with Bob
