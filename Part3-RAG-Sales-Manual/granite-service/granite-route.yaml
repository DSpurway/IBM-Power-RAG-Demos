apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: granite-service
  labels:
    app: granite-service
spec:
  to:
    kind: Service
    name: granite-service
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None

# Made with Bob
